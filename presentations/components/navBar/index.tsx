import React from "react";
import styles from "./NavBar.module.scss";

const NavBar = () => {
  return (
    <div className={styles.container}>
      <h1 className={styles.title}>FLYJET</h1>
    </div>
  );
};

export default NavBar;
